-- OUTPUTS: uniwue (DISTINCT) values only
-- show wineries that only possessing by using: plum, cherry, rose,hazelnut
-- using only singular form of the aromas

SELECT DISTINCT winery
FROM winemag_p1
WHERE LOWER(description) ~ '\y(plum|cherry|rose|hazelnut)\y'

